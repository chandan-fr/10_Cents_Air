import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const HotelGallery = ({ navigation }) => {
    return (
        <View>
            <Text>HotelGallery</Text>
        </View>
    )
};

export default HotelGallery;

const styles = StyleSheet.create({});